import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#fc8019",
            contrastText: "#FFFFFF",
        },
        secondary: {
            main: "#2b1e16",
            contrastText: "#FFFFFF",
        },
    },
});
